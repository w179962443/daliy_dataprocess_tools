# 项目架构说明

## 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     用户界面层                               │
├─────────────────────────────────────────────────────────────┤
│  Web UI (HTML/JS)        │        PyQt6 桌面应用             │
│  http://localhost:5000   │        本地窗口应用               │
└────────────┬──────────────────────────────────┬──────────────┘
             │                                  │
             └──────────────────┬───────────────┘
                                │
┌───────────────────────────────▼───────────────────────────────┐
│                    Flask Web 服务器                           │
│  (app.py)                                                     │
│  - REST API 端点                                             │
│  - Server-Sent Events (实时数据推送)                         │
│  - 会话管理                                                   │
└───────────────────────────────┬───────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
┌───────▼──────────────┐ ┌──────▼──────────────┐ ┌─────▼──────────┐
│   音频录制模块        │ │  Whisper转录模块    │ │  日志保存模块   │
│ (audio_recorder.py)  │ │(whisper_transc...) │ │(含log写入)     │
│                      │ │                    │ │                │
│ • 麦克风输入         │ │ • Whisper模型      │ │ • 文件I/O      │
│ • 缓冲管理           │ │ • 多语言识别       │ │ • 时间戳记录   │
│ • 实时音频采样       │ │ • 混合语言支持     │ │ • 会话管理     │
│ • 多音源支持         │ │ • 置信度计算       │ │ • 统计汇总     │
└────────────┬─────────┘ └────────────────────┘ └────────┬───────┘
             │                                           │
             └──────────────────┬──────────────────────┘
                                │
                    ┌───────────▼────────────┐
                    │      recordings/       │
                    │   转录文本文件         │
                    │  (按时间戳命名)       │
                    └───────────────────────┘
```

## 核心模块说明

### 1. audio_recorder.py - 音频录制模块

**职责:** 获取音频输入并进行缓冲

```python
AudioRecorder:
├── __init__()              # 初始化录制器
├── start_recording()       # 开始录制
├── stop_recording()        # 停止录制
├── audio_callback()        # 音频流回调
├── get_audio_chunk()       # 获取音频块
└── clear_buffer()          # 清空缓冲

SystemAudioRecorder:       # 系统声音录制（可选）
└── start_recording()       # 系统音频捕获
```

**关键特性:**

- 使用 `sounddevice` 库进行实时音频捕获
- 环形缓冲区 (deque) 存储音频块
- 线程安全的锁机制
- 支持多音源 (麦克风/系统/混合)

**数据流:**

```
麦克风 → sounddevice回调 → audio_buffer (deque) → get_audio_chunk()
```

### 2. whisper_transcriber.py - 转录模块

**职责:** 将音频转换为文本并管理会话

```python
WhisperTranscriber:
├── __init__()              # 初始化转录器
├── _load_model()           # 加载Whisper模型
├── transcribe_audio()      # 单次转录
└── transcribe_with_timestamps()  # 带时间戳转录

TranscriptionLogger:       # 转录日志记录器
├── __init__()              # 初始化日志器
├── start_new_session()     # 新建会话
├── log_transcription()     # 记录单条转录
├── get_session_summary()   # 获取会话摘要
└── export_session()        # 导出会话
```

**关键特性:**

- 支持 5 种 Whisper 模型 (tiny → large)
- 自动语言检测或指定语言
- 带时间戳的分段转录
- 文件自动编码为 UTF-8

**数据流:**

```
音频数据 → Whisper模型 → {text, language, segments} → logger → 文本文件
```

### 3. app.py - Flask Web 服务器

**职责:** 提供 REST API 和 Web 界面

```python
端点:
├── GET  /                  # 主页
├── POST /api/start         # 开始转录
├── POST /api/stop          # 停止转录
├── GET  /api/status        # 获取状态
├── GET  /api/transcriptions # Server-Sent Events 流
├── POST /api/config        # 修改配置
├── GET  /api/sessions      # 列出会话
└── GET  /api/download/<file> # 下载文件

后台线程:
└── transcription_worker()  # 转录处理线程
    ├── 定期获取音频块
    ├── 调用Whisper转录
    ├── 记录到日志文件
    └── 推送到前端
```

**关键特性:**

- Server-Sent Events 实现实时推送
- 线程安全的全局状态管理
- RESTful API 设计
- CORS 跨域支持

**数据流:**

```
用户请求 → Flask路由 → 后台线程 → SSE推送 → 前端实时显示
```

### 4. templates/index.html - Web 前端

**职责:** 用户界面和交互

```html
布局: ├── 顶部头部 (标题) ├── 左侧控制面板 │ ├── 启动/停止按钮 │ ├── 设置选项
(音源、语言、模型) │ ├── 统计信息 │ └── 操作按钮 └── 右侧转录显示 ├──
转录内容区域 └── 状态栏 交互: ├── 事件监听 (SSE EventSource) ├── 实时DOM更新 ├──
配置提交 └── 文件下载
```

**关键特性:**

- 响应式设计 (PC 和移动)
- 实时 SSE 连接
- 自动滚动和时间戳
- 代码转义防止 XSS

## 数据流时序图

```
时间轴:
0s:    用户点击"开始转录"
       ↓
       POST /api/start
       ↓
1s:    开始录制，启动后台线程
       recorder.start_recording()
       transcription_worker() 开始运行
       ↓
2-3s:  首次转录间隔到达
       获取音频: recorder.get_audio_chunk()
       转录: whisper.transcribe_audio(audio)
       记录: logger.log_transcription()
       推送: transcription_queue.put(result)
       ↓
     SSE推送给前端
       ↓
前端:  接收消息，更新DOM
       addTranscriptionEntry(data)
       ↓
4-6s:  重复转录和推送
       ...
       ↓
终止:  用户点击"停止转录"
       POST /api/stop
       ↓
       is_running = False
       recorder.stop_recording()
       thread.join()
       保存会话摘要
```

## 配置管理

```python
# config.py 配置层级

AUDIO_CONFIG
├── sample_rate          # 16000 Hz (标准语音频率)
├── chunk_duration       # 0.5 秒 (缓冲单位)
└── max_buffer_size      # 50 块

WHISPER_CONFIG
├── model_name           # tiny/base/small/medium/large
├── language             # auto/zh/en/etc
└── transcribe_interval  # 2 秒

FLASK_CONFIG
├── host                 # 0.0.0.0
└── port                 # 5000

STORAGE_CONFIG
├── output_dir           # recordings/
└── encoding             # utf-8
```

## 文件持久化

### 目录结构

```
realtime_transcriber/
├── app.py                      # 主程序 (约450行)
├── audio_recorder.py           # 录制模块 (约160行)
├── whisper_transcriber.py      # 转录模块 (约280行)
├── app_pyqt.py                 # 桌面版 (可选, 约600行)
├── config.py                   # 配置文件 (约120行)
│
├── templates/
│   └── index.html              # Web界面 (约550行)
├── static/                     # 静态资源 (目前为空)
│
├── recordings/                 # 转录文件目录
│   ├── transcription_20240109_143000.txt
│   ├── transcription_20240109_151530.txt
│   └── ...
│
├── requirements.txt            # Python依赖
├── README.md                   # 完整文档
├── QUICKSTART.md               # 快速开始
├── run.py                      # Python启动脚本
└── run.bat                     # Windows启动脚本
```

### 转录文件格式

**文件名:** `transcription_YYYYMMDD_HHMMSS.txt`

**内容示例:**

```
============================================================
转录会话开始时间: 2024-01-09 14:30:00
============================================================

[2024-01-09 14:30:05.123] [zh] 你好，这是一个测试
[2024-01-09 14:30:07.456] [en] Hello world
[2024-01-09 14:30:10.789] [zh] amigo 是西班牙语
[2024-01-09 14:30:13.012] [mixed] 中English混合
```

## 进程和线程模型

```
主进程 (Flask)
├── 主线程 (Web服务器)
│   └── 处理HTTP请求
│       ├── POST /api/start → 启动worker线程
│       ├── GET /api/transcriptions → SSE连接
│       └── POST /api/stop → 信号worker停止
│
└── Worker线程 (transcription_worker)
    ├── 定时器循环 (每秒10次)
    │   ├── 检查转录间隔 (每2秒)
    │   │   ├── 获取音频块 (0-500ms)
    │   │   ├── Whisper推理 (500ms-5000ms)
    │   │   ├── 文件写入 (1ms)
    │   │   └── 队列推送 (1ms)
    │   └── 进程事件 (is_running)
    │       └── False 时退出
    └── 异常捕获和日志
```

## 性能特征

### 音频处理

- **采样率**: 16 kHz (Whisper 标准)
- **缓冲大小**: 16000 × 0.5s = 8000 样本 = 128KB
- **最大缓冲**: 50 块 = 25 秒音频 = 6.4MB 内存

### Whisper 转录时间 (基准)

```
模型      输入音频长度    推理时间
tiny      30秒          2秒
base      30秒          5秒
small     30秒          15秒
medium    30秒          45秒
large     30秒          120秒
```

### Web 传输

- **SSE 心跳**: 每 0.1 秒一次
- **数据大小**: 平均 200B/转录条
- **延迟**: <100ms (本地)

## 错误处理

```
异常处理链:
├── 麦克风初始化失败
│   └── 捕获 → 显示错误消息 → 用户选择其他音源
│
├── Whisper模型加载失败
│   └── 捕获 → 显示错误 → 提示下载
│
├── 转录异常
│   └── 捕获 → 记录日志 → 继续运行
│
└── 文件写入失败
    └── 捕获 → 重试 → 失败后记录
```

## 扩展点

### 易于扩展的部分

1. **音频输入** (audio_recorder.py)

   - 添加新的音源类型
   - 集成其他音频库

2. **转录引擎** (whisper_transcriber.py)

   - 替换为其他 STT 模型
   - 添加预处理/后处理

3. **存储后端** (app.py + whisper_transcriber.py)

   - 从文件改为数据库
   - 添加云存储集成

4. **前端界面** (templates/index.html)

   - 添加实时可视化
   - 集成翻译功能

5. **API 端点** (app.py)
   - 添加批量处理接口
   - 增加权限认证

---

**这个架构设计易于理解、易于扩展、易于部署。** 🚀
