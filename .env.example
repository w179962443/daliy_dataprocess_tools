# 复制此文件为 .env 并填写你的值
# cp .env.example .env

# HuggingFace Token（说话人识别必需）
# 获取地址: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# 默认音频文件名（相对于 ./data/ 目录）
AUDIO_FILE=audio.mp3

# 模型大小: tiny / base / small / medium / large / turbo
MODEL=turbo

# 语言: zh / en / auto
LANG=zh

# 额外参数（可选），例如:
# EXTRA_ARGS=--min-speakers 2 --max-speakers 2
EXTRA_ARGS=
